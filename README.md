Лабораторные работы по ООП (1-6) (C#)

Откройте Program.cs и отредактируйте вызовы в методе Main():

static void Main()
{   
    //System.Console.WriteLine("[INFO] Start");
    
    // Оставьте активную:
    Lab6.Lab6.Run_1();  // ← текущая работа
    
    System.Console.WriteLine("[INFO] End");
}
Запустить
Нажмите F5 (Debug) или Ctrl+Shift+B (сборка)
Или через терминал: dotnet run --configuration Release
